# This file is an example; variables are read from the process environment.
SERVING_DIR=../frontend/mock
# For S3 mode, unset SERVING_DIR and set DATA_BUCKET instead.
# DATA_BUCKET=your-existing-private-data-bucket
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
PORT=8000
